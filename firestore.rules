
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // This rule allows anyone to read and write to any collection.
    // This is a temporary measure to ensure the application is functional
    // before implementing more granular security rules.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
